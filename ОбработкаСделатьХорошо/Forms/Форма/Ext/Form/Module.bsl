&НаКлиенте
Процедура СделатьХорошо(Команда)
	
	ЭтаФорма.ВключенРежимХорошо = НЕ ЭтаФорма.ВключенРежимХорошо;
	
	Если ЭтаФорма.ВключенРежимХорошо Тогда
		
		ПодключитьОбработчикОжидания("ИзменитьЦветФонаКнопки", 1, Ложь);
		
	Иначе
		
		ОтключитьОбработчикОжидания("ИзменитьЦветФонаКнопки");
		Элементы.СделатьХорошо.ЦветФона = ПолучитьЦветФонаКнопкиПоУмолчанию();
		
	КонецЕсли;

КонецПроцедуры

&НаСервере
Функция ПолучитьЦветФонаКнопкиПоУмолчанию()
	
	Возврат ЦветаСтиля.ЦветФонаКнопки;
	
КонецФункции

&НаКлиенте
Функция ИзменитьЦветФонаКнопки() Экспорт
	
	ЭтаФорма.Счетчик = ЭтаФорма.Счетчик + 1;
	
	Если ЭтаФорма.Счетчик%9 = 1 Тогда
		Элементы.СделатьХорошо.ЦветФона = WebЦвета.СветлоКоралловый;
	ИначеЕсли ЭтаФорма.Счетчик%9 = 2 Тогда
		Элементы.СделатьХорошо.ЦветФона = WebЦвета.Голубой;
	ИначеЕсли ЭтаФорма.Счетчик%9 = 3 Тогда
		Элементы.СделатьХорошо.ЦветФона = WebЦвета.СветлоЗеленый;
	ИначеЕсли ЭтаФорма.Счетчик%9 = 4 Тогда
		Элементы.СделатьХорошо.ЦветФона = WebЦвета.СветлоЖелтый;
	ИначеЕсли ЭтаФорма.Счетчик%9 = 5 Тогда
		Элементы.СделатьХорошо.ЦветФона = WebЦвета.БледноКрасноФиолетовый;
	ИначеЕсли ЭтаФорма.Счетчик%9 = 6 Тогда
		Элементы.СделатьХорошо.ЦветФона = WebЦвета.АнтикБелый;
	ИначеЕсли ЭтаФорма.Счетчик%9 = 7 Тогда
		Элементы.СделатьХорошо.ЦветФона = WebЦвета.БледноМиндальный;
	ИначеЕсли ЭтаФорма.Счетчик%9 = 8 Тогда
		Элементы.СделатьХорошо.ЦветФона = WebЦвета.БледноЛиловый;	
	ИначеЕсли ЭтаФорма.Счетчик%9 = 0 Тогда
		Элементы.СделатьХорошо.ЦветФона = WebЦвета.Желтый;
	КонецЕсли;
	
КонецФункции